services:
  - type: web
    name: quickbuy-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s build
    envVars:
      - key: NODE_VERSION
        value: 18.18.0
    autoDeploy: true
